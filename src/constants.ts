export const SNAP_THRESHOLD = 20; // in px
export const MIN_WINDOW_WIDTH = 10; // in percent
export const MIN_WINDOW_HEIGHT = 10; // in percent
export const HEADER_HEIGHT = 24; // in px

export const IS_CLOSABLE = true;
export const IS_EXPANDABLE = true;
